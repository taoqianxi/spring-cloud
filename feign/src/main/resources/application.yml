server:
  port: 8003
spring:
  application:
    name: feign
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka/
  instance:
    prefer-ip-address: true #注册ip到eureka
feign:
  hystrix:
    enabled: true #开启服务熔断